Multiple inheritance is used when the user want to inherit the properties of multiple classes.
The keyword used is implements.The interface is a file which contains all the methods.

Static and Default methods can also be used in Interfaces

Sample Program:
---------------

package selenium;

public class interfacepractise implements Interfaceone{
	
	public static void main(String[]Args) {
		
		
		interfacepractise object = new interfacepractise();
		
		object.add(100, 20);
		object.subtract(100, 20);
		
	}
	@Override
	public void add(int a, int b) {
		// TODO Auto-generated method stub
		
		int sum = a+b;
		
		System.out.println("This is add from Interface" +" "+sum);
		
	}

	@Override
	public void subtract(int a,int b) {
		// TODO Auto-generated method stub
		
		int sub = a-b;
		
		System.out.println("This is subtract from Interface"+" " +sub);
		
	}

}
